name: space_time
description: Classes that handle time, geo-location, coordinates ,,,etc
version: 0.0.2
publish_to: "none"
homepage: https://github.com/RagehAz/space_time.git
# --------------------------------------------------------------------------------
environment:
  sdk: ">=2.10.5 <3.0.0"
  flutter: ">=1.17.0"
# --------------------------------------------------------------------------------
dependencies:
  flutter:
    sdk: flutter
# ------------------------------------
  cloud_firestore: ^4.0.3
  intl: ^0.17.0
  # ------------------------------------
  mapper:
    git:
      url: https://github.com/RagehAz/mapper.git
  numeric:
    git:
      url: https://github.com/RagehAz/numeric.git
  stringer:
    git:
      url: https://github.com/RagehAz/stringer.git
# --------------------------------------------------------------------------------
dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.0.0
# --------------------------------------------------------------------------------
flutter:
# --------------------------------------------------------------------------------
